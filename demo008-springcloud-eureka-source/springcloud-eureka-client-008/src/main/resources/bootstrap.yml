server:
  port: 8001
spring:
  application:
    name: springcloud-eureka-client-008
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:7001}/eureka/
  instance:
    prefer-ip-address: true
    instance-id: springcloud-eureka-client-008